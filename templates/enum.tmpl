// 枚举类型定义
{{- range .Enums }}
/**
 * {{ .SchemaName }}
 */
export enum {{ .TypeName }} {
{{- range $index, $value := .EnumValues }}
  {{ $value }}{{- if not (eq $index (len $.EnumValues)) }},{{ end }}
{{- end }}
}
{{- end }}
